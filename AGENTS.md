# Project Guidelines & Rules

- **Preserve Google Services Config**: NEVER delete, remove, or ignore `google-services.json` (at root `/google-services.json` and `/app/google-services.json`) during file operations, builds, staging, or committing.
